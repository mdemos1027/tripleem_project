# TripleEM Backend API

## Setup

1. Copy `.env.example` to `.env` and fill in the values.
2. Run `npm install`
3. Start the server:

```bash
npm run dev
```

## API Endpoints

- `GET /health` → health check
